created using:

tensorflow==2.15.0 (trained the model first)
!pip install tensorflowjs==4.17.0 (maybe can even go higher)
!pip install tensorflow==2.15.0 (downgraded)
!pip install tensorflow-decision-forests==1.8.1 (compatibility)